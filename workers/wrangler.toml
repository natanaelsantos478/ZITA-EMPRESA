# Cloudflare Pages — ZITA Escritório de IA (React App)
# Build command: npm run build  (configurar no dashboard do Cloudflare Pages)
name = "escritorio"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"
